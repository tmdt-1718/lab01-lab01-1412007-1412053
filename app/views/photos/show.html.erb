<div class="text-center"><img class="img-fluid" src="<%= @photo.image %>"></div>

<!-- <h1 class="mt-3"><a href="/albums/<%=@album.id%>"><%= @album.title %></a></h1> -->

<% if @photo.album.user.id == session[:current_user_id] %>
<div class="text-center h6 mt-2">
	<%= link_to photo_path, class: 'card-link', remote: true, method: :delete do %> 
	Delete
</div>
<% end %>
<% else %>
<% end %>

<h4 class="text-muted text-center mt-3">
	<%=@album.user.name%></h4>

	<% if @photo.view.nil? %>
	<h5 class="text-muted text-center">0 views</h5>
	<% else %>
	<h5 class="text-muted text-center"><%= @photo.view %> views</h5>
	<% end %>

	<h5 class="mt-4 text-center"><a href="/albums/<%=@album.id%>">Back to <%= @album.title %></a></h1>

		<div class="mb-4"></div>